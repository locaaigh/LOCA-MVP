# Resumen sesión 2026-08-02 (handoff para retomar)

Sesión larga: se ejecutó todo el backlog v2 (ver `PLAN-v2.md` para el detalle por item y estado). Acá va lo último, qué chequear y los next steps para tener el MVP con usuarios reales esta semana.

## Lo último que hicimos (esta sesión)
Todo con typecheck + build verdes; dev server en http://localhost:3000.

1. **Imagen limpia en la preview** (`content-preview.tsx`): se sacó el texto/CTA/marca superpuestos. Ahora se muestra la imagen REAL, limpia y en su **proporción original sin recortar** (lo que se aprueba = lo que se publica). La red social se indica con el logo (lo pone la card).
2. **"Cambiar imagen" ahora regenera de verdad** (`content-review.tsx`): antes era un stub que solo guardaba feedback. Ahora ajusta el imagePrompt con el feedback, borra la imagen vieja, muestra "Eva trabajando" y regenera.
3. **Onboarding paso 3 (identidad visual)**: la validación era demasiado estricta (exigía color "principal" y "tono de voz" puntual) → se hizo intuitiva (alcanza con algún color, alguna tipografía y algún tono/estilo). Foto del profesional sigue requerida.
4. **Navegación a campo faltante**: al tocar "Siguiente" con obligatorios incompletos, ahora scrollea + enfoca el campo exacto y el toast dice cuál. Se agregaron anclas en pasos 3 y 4.
5. **Export "Pack de contenidos" (.zip)**: imagen + copy `.txt` + `_resumen.csv`. En **Aprobados** (todas) y **Publicados** (con rango de fechas). Nueva dep: `jszip`.

(Antes en la sesión: los 25 items del backlog — onboarding, contenidos, publicación real Meta, settings, etc. Ver PLAN-v2.)

## Qué hay que CHEQUEAR (verificación pendiente)
1. 🔴 **CRÍTICO — ¿IA real o mock?** Los model IDs (`ANTHROPIC_MODEL=claude-sonnet-4-6`, `GEMINI_IMAGE_MODEL=gemini-3-pro-image`) parecen inválidos. Test: entrar **logueado (no /demo)**, generar, y mirar el contador 🪙 abajo a la derecha. Si queda en **$0 / no aparece** → está cayendo a MOCK. Es lo primero a resolver. (Modelos reales: claude-sonnet-4-5 o Claude 5; para imágenes confirmar el ID vigente de Gemini.)
2. Probar en la app los fixes de hoy: cambiar imagen regenera y oculta la anterior; export pack baja bien; onboarding paso 3 avanza; imágenes se ven enteras/sin texto encima.
3. ¿Migraciones de Supabase 0001→0005 aplicadas en la base real? (script `scripts/verify-supabase-schema.mts`).
4. Confirmar que las imágenes generadas quedan con **URL pública** en Supabase Storage (necesario para publicar en IG).

## NEXT STEPS para MVP con usuarios reales ESTA SEMANA
Camino pragmático: **generar → revisar → aprobar → exportar pack (publicación manual)**. Auto-publicación en redes queda para fase 2 (requiere App Review de Meta, que es lento).

**Bloqueantes (hacer sí o sí):**
1. **Arreglar los model IDs** → sin esto no hay IA real. (verificación #1 de arriba).
2. **Deploy a producción**: Vercel + env vars reales + dominio de LOCA activo (también necesario para LinkedIn/Meta redirect URIs).
3. **Auth/sesiones para multiusuario real**: revisar P1.5 (middleware guard en rutas `(app)/*`, re-hidratar en SIGNED_IN) — ver `PLAN-parte1.md`.
4. **Flujo end-to-end probado con IA real**: onboarding → estrategia → contenidos → aprobar → **export pack**. Que un usuario real lo complete sin trabarse.

**Empezar YA porque son lentos (en paralelo):**
5. **Meta App Review** de `instagram_content_publish` / `pages_manage_posts` (para publicar en cuentas de usuarios, no solo admin). Sin esto, auto-publicación real no funciona para clientes.
6. **LinkedIn**: crear la app (dueña = **INFINIDAD**, nombre visible = **LOCA**) y pedir el producto de Company Pages para que corra la revisión. Mientras, probar con perfil personal (`w_member_social`, aprobación inmediata). Guía en el chat de esta sesión (ver también si se guardó `docs/LINKEDIN-SETUP.md`).

**Después del lanzamiento (fase 2):**
7. Cron de auto-publicación programada (Vercel Pro / scheduler externo) — item A5.
8. Bloque de **agentes** (item 21, pausado): verificar/rutear modelos + prompt caching + prompting por tipo de contenido + **aspect ratio correcto por formato** + **reglas de formato por red** (IG feed 1:1/4:5 no 9:16; story 9:16; reel 9:16; stories interactivas NO por API). Todo detallado en PLAN-v2 sección ITEM 21.
9. Email real de avisos (D22), botón hoy inactivo.

## Estado técnico
- typecheck ✓ · build ✓ · dev server ✓ (localhost:3000).
- Deps nuevas esta sesión: `jszip`.
- Fuente de verdad del backlog y pendientes: `PLAN-v2.md`.
